{
  "name": "techzu-e2e-tests",
  "version": "1.0.0",
  "description": "High-concurrency Playwright API tests for the Techzu Inventory System",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=list",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
